{{define "include"}}

import (
    {{- range $included := .}}
    {{index $included 0}} "{{index $included 1}}"
    {{- end}}
)

{{- range $included := .}}
var _ = {{index $included 0}}.GoUnusedProtection__
{{- end}}
{{- end}}
