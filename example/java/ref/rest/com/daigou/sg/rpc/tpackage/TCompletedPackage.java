package com.daigou.sg.rpc.tpackage;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonToken;

import java.io.IOException;
import java.io.Serializable;
import java.util.ArrayList;

/**
 * This file is auto-generated by tgen
 * Don't change manually
 */

public class TCompletedPackage implements Serializable {
    public int customerCommentId;
    public String satisfaction;
    public String caption;
    public String comment;
    public ArrayList<TPackage> packages;

    static TCompletedPackage fromJSON(JsonParser jp) throws IOException {
        // Sanity check: verify that we got "Json Object":
        if (jp.nextToken() != JsonToken.START_OBJECT) {
            throw new IOException("Expected data to start with a TCompletedPackage Object");
        }

        return doFromJSON(jp);
    }

    private static TCompletedPackage doFromJSON(JsonParser jp) throws IOException {
        TCompletedPackage result = new TCompletedPackage();

        // Iterate over object fields:
        while (jp.nextToken() != JsonToken.END_OBJECT) {
            String fieldName = jp.getCurrentName();

            if (false) {
            } else if (fieldName.equals("customerCommentId")) {
                jp.nextToken();
                result.customerCommentId = jp.getIntValue();
            } else if (fieldName.equals("satisfaction")) {
                jp.nextToken();
                result.satisfaction = jp.getText();
            } else if (fieldName.equals("caption")) {
                jp.nextToken();
                result.caption = jp.getText();
            } else if (fieldName.equals("comment")) {
                jp.nextToken();
                result.comment = jp.getText();
            } else if (fieldName.equals("packages")) {
                result.packages = TPackage.fromJSONArray(jp);
            }
        }
        return result;
    }

    static ArrayList<TCompletedPackage> fromJSONArray(JsonParser jp) throws IOException {
        if (jp.nextToken() != JsonToken.START_ARRAY) {
            throw new IOException("Expected data to start with a TCompletedPackage array");
        }

        ArrayList<TCompletedPackage> result = new ArrayList<TCompletedPackage>();

        // Iterate over object fields:
        while (jp.nextToken() != JsonToken.END_ARRAY) {
            result.add(doFromJSON(jp));
        }
        return result;
    }
}
