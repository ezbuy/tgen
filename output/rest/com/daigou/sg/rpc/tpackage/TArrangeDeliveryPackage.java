package com.daigou.sg.rpc.tpackage;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonToken;

import java.io.IOException;
import java.io.Serializable;
import java.util.ArrayList;

/**
 * This file is auto-generated by tgen
 * Don't change manually
 */

public class TArrangeDeliveryPackage implements Serializable {
    public int arrivedCount;
    public int notArrivedCount;
    public ArrayList<TPackage> packages;

    static TArrangeDeliveryPackage fromJSON(JsonParser jp) throws IOException {
        // Sanity check: verify that we got "Json Object":
        if (jp.nextToken() != JsonToken.START_OBJECT) {
            throw new IOException("Expected data to start with a TArrangeDeliveryPackage Object");
        }

        return doFromJSON(jp);
    }

    private static TArrangeDeliveryPackage doFromJSON(JsonParser jp) throws IOException {
        TArrangeDeliveryPackage result = new TArrangeDeliveryPackage();

        // Iterate over object fields:
        while (jp.nextToken() != JsonToken.END_OBJECT) {
            String fieldName = jp.getCurrentName();

            if (false) {
            } else if (fieldName.equals("arrivedCount")) {
                jp.nextToken();
                result.arrivedCount = jp.getIntValue();
            } else if (fieldName.equals("notArrivedCount")) {
                jp.nextToken();
                result.notArrivedCount = jp.getIntValue();
            } else if (fieldName.equals("packages")) {
                result.packages = TPackage.fromJSONArray(jp);
            }
        }
        return result;
    }

    static ArrayList<TArrangeDeliveryPackage> fromJSONArray(JsonParser jp) throws IOException {
        if (jp.nextToken() != JsonToken.START_ARRAY) {
            throw new IOException("Expected data to start with a TArrangeDeliveryPackage array");
        }

        ArrayList<TArrangeDeliveryPackage> result = new ArrayList<TArrangeDeliveryPackage>();

        // Iterate over object fields:
        while (jp.nextToken() != JsonToken.END_ARRAY) {
            result.add(doFromJSON(jp));
        }
        return result;
    }
}
